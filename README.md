# SilentBell
Backend en nodejs + express + mongodb

## Installation

### Arrancar docker
```bash
docker compose -f "docker-compose.yaml" up -d --build
```

###  Use
```
http://localhost:8080
```

## Endpoints
